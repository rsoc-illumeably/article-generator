# ---------------------------------------------------------------------------
# Environment variable template.
# Copy this file to .env and fill in real values before running the service.
# NEVER commit .env — it is listed in .gitignore.
# ---------------------------------------------------------------------------

# Anthropic API key — used by the LLM client to call Claude.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# API key for the POST /api/generate endpoint.
# Passed by callers as the X-API-Key request header.
API_KEY=your_api_key_here

# Password for the browser UI session.
# Entered once in the browser to unlock the UI until the tab is closed.
FRONTEND_SESSION_PASSWORD=your_frontend_password_here
