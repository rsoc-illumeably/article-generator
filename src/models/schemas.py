"""Pydantic request and response schemas.

GenerateRequest   — incoming payload for POST /api/generate
GenerateResponse  — success response (article + metadata)
ErrorResponse     — failure response (error, iteration count, reasoning chain)
IterationRecord   — per-iteration agent trace included in verbose/error responses
"""

# TODO: implement
